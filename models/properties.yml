sources:
  - name: "source table's info"
    database: CLASS4
    schema: ASSIGNMENT  
    tables:
      - name: raw_users
      - name: raw_country

models:
  - name: users_country_combine_data
    description: "Combines user data with country data"
    config:
      materialized: 'table'


